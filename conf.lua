local SCREEN_WIDTH = 1500
local SCREEN_HEIGHT = 750

function love.conf(t)
    t.title = "Relics of Elboria"
    t.console = false
    t.window.width = SCREEN_WIDTH
    t.window.height = SCREEN_HEIGHT
end